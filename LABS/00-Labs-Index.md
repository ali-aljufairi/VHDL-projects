# Labs index

These folders were renamed to descriptive names for easier navigation.

- **alu-ops-lab1/** — ALU operations selected by opcode
- **register-file-lab2/** — register file (write + read)
- **condition-decoder-lab3/** — instruction/decoder + condition logic
- **dual-clock-ram-lab4/** — RAM with separate read/write clocks
