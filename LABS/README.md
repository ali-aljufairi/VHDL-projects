# LABS

Lab exercises / submissions.

## How to use
- Each lab folder should ideally contain:
  - the design files (`*.vhd` / `*.vhdl`)
  - a testbench (`tb_*.vhd`) when possible
  - (optional) a short note on expected behavior

If you are new here, you may find the **Projects/** folder easier to navigate because it is more consistently structured.
