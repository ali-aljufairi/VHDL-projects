# scripts

Helper scripts for working with the repo.

- `ghdl_syntax_check.sh` — runs a fast syntax-only pass (`ghdl -s`) over all VHDL files.

These scripts are intentionally minimal and should work on Linux/macOS.
